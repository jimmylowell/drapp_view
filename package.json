{
  "name": "drapp-view",
  "private": true,
  "description": "Dev tooling only — the site is build-free static HTML/JS. Never import node_modules from js/.",
  "scripts": {
    "probe": "node scripts/probe_sources.mjs",
    "smoke": "node scripts/smoke_dom.mjs",
    "serve": "python3 -m http.server 8765"
  },
  "devDependencies": {
    "jsdom": "^25.0.0"
  }
}
